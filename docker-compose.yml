version: '3'

services:
  rr:
    container_name: rottingresearch
    image: rottingresearch/rottingresearch
    command: python app.py && celery -A app:celery_app worker -l info
    depends_on:
      - "redis"
    restart: unless-stopped
    ports:
      - "8080:8080"
    environment:
      - FLASK_APP=app
      - APP_SECRET_KEY=change_me
      - REDIS_URL=redis://redis:6379
      - TMP_CUSTOM_DIR=/tmp_dir/
    volumes:
      - app:/app
      - cache_vol:/tmp_dir/

  redis:
    image: "redis:6.0.5-buster"
    volumes:
      - "redis:/data"

volumes:
  redis: {}
  cache_vol:
    driver_opts:
      type: tmpfs
      device: tmpfs
  app:
    driver_opts:
      type: tmpfs
      device: tmpfs
